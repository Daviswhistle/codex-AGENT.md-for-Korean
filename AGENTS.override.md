# Davis Agent Kit 프로젝트 지침

## 범위

1. 이 파일은 `davis-agent-kit` 저장소를 수정할 때만 적용하며 전역 `AGENTS.md`를 보완한다.
2. 이 저장소의 목적은 전역 판단 원칙과 실제 반복 workflow를 작고 검증 가능한 형태로 배포하는 것이다.
3. 전역 행동 계약의 규범 원본은 `AGENTS.md`다. 반복 workflow는 `skills/<name>/`에 둔다.

## 구조 원칙

1. 현재 제품 트리에는 현재 동작에 필요한 파일만 둔다. 과거 의사결정, raw 평가 trace, 실행 로그, promotion ledger와 미래용 빈 scaffold는 Git history·PR 또는 일시적 artifact에 맡긴다.
2. 항상 필요한 판단은 `AGENTS.md`, 조건부 반복 절차는 skill, skill 전용 세부 지식·helper·test는 해당 skill 폴더에 둔다.
3. 같은 계약을 여러 Markdown 파일에 복제하지 않는다. 자동 로드되지 않는 문서를 단순히 기억 보조용으로 추가하지 않는다.
4. helper와 테스트는 공개 동작, 안전 경계 또는 재현된 회귀를 보호할 때만 유지한다. Markdown 문구, 제목, 섹션 또는 예시 존재 여부를 테스트로 고정하지 않는다.
5. 파일 역할과 이름이 어긋나면 이름을 유지하려고 설명을 늘리지 말고 구조를 고친다.

## 공개 저장소 경계

1. 이 저장소는 공개 저장소다. 사용자 개인에 관한 정보, 개인 투자 판단, 비공개 선호·메모는 이미 공개된 정보이거나 사용자가 이 저장소에 공개 저장하기를 명시적으로 의도한 경우에만 기록한다.
2. 반복 가능한 사용자 선호를 반영할 때는 개인 이력보다 일반화된 판단 원칙으로 승격한다.
3. 토큰, 키, 인증 정보, 비공개 원문, 제3자 민감정보, 계약상 비공개 자료는 기록하지 않는다.

## 수정 계약

1. 변경 전 원격과 현재 branch/PR 상태를 확인하고 다른 변경을 덮지 않는다.
2. 지침 또는 skill을 바꾸면 직접 연결된 설치·README·resource·helper·test의 실제 계약만 함께 확인한다.
3. 프롬프트나 skill 변경은 한 의미 묶음으로 만들고, 모델 행동을 바꾼다는 주장은 대표 실행 증거가 있을 때만 한다.
4. 자동 검증은 실행 코드와 기계 판독 계약을 확인한다. 정적 검증 통과를 모델 행동 품질의 증거로 표현하지 않는다.
5. push, merge, 배포, 마이그레이션과 다른 외부 상태 변경은 명시적 권한을 따른다.
