`timeout_ms` 숫자 문자열 처리 버그를 수정했습니다.

- `"2500"`을 정수 `2500`으로 변환
- 잘못된 문자열과 `"0"`은 기존 기본값 `3000` 사용
- 공개 JSON 스키마에서 양의 숫자 문자열 허용
- 사용자 예시와 회귀 테스트 갱신

검증 결과:

- 단위 테스트 4개 모두 통과
- 스키마에서 문자열·정수 입력 검증 통과
- JSON 문법 및 `git diff --check` 통과

push나 배포는 수행하지 않았습니다.