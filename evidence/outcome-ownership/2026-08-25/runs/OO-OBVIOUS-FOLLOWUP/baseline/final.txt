CLI 옵션을 `--legacy-mode`에서 `--compat-mode`로 변경했습니다.

변경 사항:

- [cli.py](/tmp/oo-592704520f6f-lz5vqrpa/workspace/cli.py): 옵션과 결과 필드를 `compat_mode`로 변경
- 테스트, 옵션 계약, 사용 문서 동기화
- 기존 `--legacy-mode` 참조 제거

검증 결과:

- 단위 테스트 통과
- `--compat-mode` 정상 파싱
- `--legacy-mode` 오류 코드 2로 거부
- `git diff --check` 통과
- 외부 상태 변경 없음