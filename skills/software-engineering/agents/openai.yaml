interface:
  display_name: "Software Engineering & Repository Analysis"
  short_description: "Retrieve repository evidence and orchestrate software changes"
  default_prompt: "Use $software-engineering to choose the narrowest repository retrieval route. For software changes, define the task contract, delegate non-trivial implementation to a worker, verify the result, and decide whether CRA or TCA is warranted."
